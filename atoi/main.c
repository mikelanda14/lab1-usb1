#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
int main()
{
    char numero[5];
    int num;
    printf("ingrese un numero");
    scanf("%s",numero);
    for (i=0;i<5;i++){

        if(!isdigit(tnumero[i])){
            printf("el dato contiene no digitos");

        }
        else {
            num=atoi
        }
    }
}
